---
interface Props {
  href: string;
  variant?: 'primary' | 'outline';
}
const { href, variant = 'primary' } = Astro.props;
---

<a
  href={href}
  class:list={[
    'inline-flex items-center justify-center px-5 py-2.5 rounded-full text-sm font-semibold transition-all duration-200 cursor-pointer min-h-[44px]',
    variant === 'primary'
      ? 'bg-[var(--color-accent)] text-white btn-glow transition-shadow duration-300'
      : 'border border-purple-500/30 text-[var(--color-text)] hover:border-[var(--color-accent)] hover:text-[var(--color-accent)] hover:bg-purple-500/5',
  ]}
>
  <slot />
</a>
